version: '3'
services:
  web:
    build: .
    ports:
      - "3000:3000"
    links: [ mongo ]
  mongo:
    image: "mongo"
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
volumes: 
  mongo_data:
    external: true